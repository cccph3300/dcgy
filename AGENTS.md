# 项目目录说明

## 总体说明

本项目同时包含东成果业的前端、小程序、后端发布项目以及 APP 打包证书相关目录。后续修改代码时，必须先确认当前工作目录和目标端，避免把小程序、H5、APP 或后端的改动混在一起。

## 目录职责

- `dcgy/miniapp`
  - 前端与小程序工程目录。
  - 同一套 uni-app 代码需要同时兼容 H5 网页端、微信小程序端和 APP 端。
  - 修改页面、样式、路由、接口调用、小程序分包、APP 打包配置时，主要在这里处理。
  - 所有界面风格需要保持东成果业统一的绿色水果风。

- `dcgy_server_publish`
  - 后端发布项目目录。
  - 负责业务 API、订单、库存、超市配送、利润、打印、OCR 代理等服务端能力。
  - 前端或小程序出现接口缺失、跨端网络限制、第三方服务转发等问题时，优先在这里补后端接口，而不是让小程序直接绕限制访问公网 IP。

- `dcgy_keys`
  - APP 程序证书和密钥存放目录。
  - 例如 Android 自有证书、签名 keystore、证书密码相关资料。
  - 这是敏感目录，不应该提交到 Git，也不要在普通代码改动中复制、重命名或移动证书文件。
  - 打包 APP 时只读取这里的证书路径，不把证书内容写进源码。

## 双端适配要求

- `miniapp` 的改动默认需要同时考虑 H5、微信小程序和 APP。
- 小程序正式版不能直接请求未备案 IP、`localhost` 或普通 HTTP 地址。
- 临时绕过小程序域名限制时，可以使用腾讯云 AnyService 或后端代理。
- H5/APP 可以按实际部署情况直连后端服务，但不要因此破坏小程序端。
- 页面滚动、底部固定栏、安全区、分包路径和静态资源大小都要兼顾微信小程序限制。

## 编码与提交约束

- 所有源码与配置文件使用 UTF-8 无 BOM。
- 中文注释、中文文案必须保持可读，不允许乱码。
- 不要提交 `miniapp/unpackage/` 等构建产物。
- 不要提交证书、密钥、密码文件。
- 修改前端后，至少构建一次微信小程序或对应目标端。
- 修改后端后，至少执行一次后端构建。
